<Window x:Class="OpenQuickHost.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择进程"
        Width="420"
        MinHeight="280"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindow}">
    <Border Margin="1"
            CornerRadius="0"
            Background="#F2161616"
            BorderBrush="#22FFFFFF"
            BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="DescriptionText"
                       Grid.Row="0"
                       Margin="0,0,0,10"
                       Style="{StaticResource DescriptionText}" />
                       
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <ItemsControl x:Name="BlacklistItemsControl" Margin="0,0,0,10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#33FFFFFF" CornerRadius="4" Margin="0,0,8,8" Padding="8,4">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ProcessName}" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White" />
                                        <Button Content="✕" Width="16" Height="16" Click="RemoveBlacklistItem_Click" Padding="0" Foreground="#FFAAAAAA" FontSize="10" Background="Transparent" BorderThickness="0" Cursor="Hand" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Grid VerticalAlignment="Top">
                        <ComboBox x:Name="ProcessComboBox"
                                  Height="40"
                                  IsEditable="True"
                                  IsTextSearchEnabled="True"
                                  StaysOpenOnEdit="True"
                                  TextSearch.TextPath="ProcessName"
                                  DisplayMemberPath="" 
                                  TextBoxBase.TextChanged="ProcessComboBox_TextChanged"
                                  SelectionChanged="ProcessComboBox_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ProcessName}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        
                        <TextBlock x:Name="PlaceholderTextBlock"
                                   Text="下拉选择"
                                   Foreground="#7FFFFFFF"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"
                                   Visibility="Visible"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="ErrorText"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="#FFEF4444"
                           FontSize="{DynamicResource FontSizeSmallPlus}"
                           TextWrapping="Wrap"
                           Visibility="Collapsed" />
                <Button Grid.Column="1"
                        Content="取消"
                        Style="{StaticResource SecondaryBtn}"
                        Width="90"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click" />
                <Button Grid.Column="2"
                        Content="确认"
                        Style="{StaticResource PrimaryBtn}"
                        Width="90"
                        Click="OkButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
