<Window x:Class="OpenQuickHost.WindowBindingDropOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="拖拽绑定窗口"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#01000000"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop"
        DragLeave="Window_DragLeave">
    <Canvas x:Name="RootCanvas">
        <Border x:Name="CornerMarker"
                Width="260"
                Height="42"
                CornerRadius="14"
                Background="#E60B1220"
                BorderBrush="#FF67E8F9"
                BorderThickness="2"
                IsHitTestVisible="False"
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="8" Opacity="0.45" />
            </Border.Effect>
            <TextBlock x:Name="CornerText"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center" />
        </Border>

        <!-- Drag label: follows cursor, shows extension name -->
        <Border x:Name="DragLabelContainer"
                CornerRadius="6"
                Background="#CC1A1A2E"
                Padding="8,4"
                IsHitTestVisible="False"
                Visibility="Collapsed">
            <TextBlock x:Name="DragLabel"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="180" />
        </Border>

        <!-- Preview icon: shows at the target binding position -->
        <Border x:Name="PreviewIcon"
                Width="34"
                Height="34"
                CornerRadius="10"
                Background="#803B82F6"
                ClipToBounds="True"
                IsHitTestVisible="False"
                Opacity="0.5"
                Visibility="Collapsed">
            <Grid>
                <Image x:Name="PreviewIconImage"
                       Stretch="UniformToFill"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Visibility="Collapsed" />
                <Viewbox x:Name="PreviewIconVector"
                         Margin="6"
                         Visibility="Collapsed">
                    <Path x:Name="PreviewIconPath"
                          Width="24"
                          Height="24"
                          Fill="White"
                          Stretch="Uniform" />
                </Viewbox>
                <TextBlock x:Name="PreviewIconGlyph"
                           FontSize="14"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontWeight="Bold"
                           Visibility="Collapsed" />
            </Grid>
        </Border>
    </Canvas>
</Window>
