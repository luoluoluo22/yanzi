<Application x:Class="OpenQuickHost.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OpenQuickHost">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- Premium Dark Theme Colors -->
        <Color x:Key="ColorBG">#FF161616</Color>
        <Color x:Key="ColorBorder">#FF2A2A2A</Color>
        <Color x:Key="ColorAccent">#FF3B82F6</Color>
        <Color x:Key="ColorTextMain">#FFFFFFFF</Color>
        <Color x:Key="ColorTextSec">#FF8E8E8E</Color>
        <Color x:Key="ColorInputBG">#FF1E1E1E</Color>

        <SolidColorBrush x:Key="BrushBG" Color="{StaticResource ColorBG}"/>
        <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}"/>
        <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}"/>
        <SolidColorBrush x:Key="BrushTextMain" Color="{StaticResource ColorTextMain}"/>
        <SolidColorBrush x:Key="BrushTextSec" Color="{StaticResource ColorTextSec}"/>
        <SolidColorBrush x:Key="BrushInputBG" Color="{StaticResource ColorInputBG}"/>

        <!-- Global Text Styles -->
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="DescriptionText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF8E8E8E"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Global Button Styles -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#FF3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#FF252525"/>
            <Setter Property="BorderBrush" Value="#FF333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FFAAAAAA"/>
        </Style>

        <!-- Global Input Style -->
        <Style x:Key="BaseInput" TargetType="Control">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="BorderBrush" Value="#FF2A2A2A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource BaseInput}">
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#FF3B82F6"/>
            <Setter Property="SelectionOpacity" Value="0.35"/>
        </Style>

        <Style TargetType="PasswordBox" BasedOn="{StaticResource BaseInput}">
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Global Window Style for Dialogs -->
        <Style x:Key="DialogWindow" TargetType="Window">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="WindowStyle" Value="None"/>
        </Style>

        <!-- Modern Dark ContextMenu Style -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#F21A1A1A"/>
            <Setter Property="BorderBrush" Value="#25FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.5"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="ContextMenuScrollViewer" 
                                          Margin="1"
                                          VerticalScrollBarVisibility="Disabled"
                                          HorizontalScrollBarVisibility="Disabled">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="Margin" Value="4,4"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="{TemplateBinding Height}" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="true"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="border"
                                Background="Transparent"
                                BorderThickness="0"
                                CornerRadius="8"
                                Margin="4,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <ContentPresenter x:Name="Icon" 
                                                 ContentSource="Icon"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 Width="18" Height="18"
                                                 Opacity="0.9"/>
                                
                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  VerticalAlignment="Center"
                                                  Margin="6,0,25,0">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=MenuItem}}"/>
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>

                                <TextBlock Grid.Column="2"
                                           x:Name="InputGestureText"
                                           Text="{TemplateBinding InputGestureText}"
                                           Foreground="#80FFFFFF"
                                           FontSize="11"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#26FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tray Context Menu Resource -->
        <ContextMenu x:Key="TrayContextMenu">
            <MenuItem Header="显示面板" Click="TrayShow_Click">
                <MenuItem.Icon>
                    <Path Data="M21,16.5C21,16.88 20.79,17.21 20.47,17.38L12.57,21.82C12.41,21.94 12.21,22 12,22C11.79,22 11.59,21.94 11.43,21.82L3.53,17.38C3.21,17.21 3,16.88 3,16.5V7.5C3,7.12 3.21,6.79 3.53,6.62L11.43,2.18C11.59,2.06 11.79,2 12,2C12.21,2 12.41,2.06 12.57,2.18L20.47,6.62C20.79,6.79 21,7.12 21,7.5V16.5Z" Fill="#A0FFFFFF" Stretch="Uniform" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="隐藏面板" Click="TrayHide_Click">
                <MenuItem.Icon>
                    <Path Data="M12,2L4.5,20.29L5.21,21L12,18L18.79,21L19.5,20.29L12,2Z" Fill="#60FFFFFF" Stretch="Uniform" Opacity="0.6"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="设置" Click="TraySettings_Click">
                <MenuItem.Icon>
                    <Path Data="M12,15C13.6569,15 15,13.6569 15,12C15,10.3431 13.6569,9 12,9C10.3431,9 9,10.3431 9,12C9,13.6569 10.3431,15 12,15Z M19.4,15L21.7,13.5C22.1,13.2 22.2,12.7 21.9,12.3L19.9,8.9C19.7,8.5 19.2,8.4 18.8,8.5L16.2,9.6C15.7,9.2 15.1,8.9 14.5,8.7L14.1,5.9C14.1,5.4 13.7,5.1 13.2,5.1L9.2,5.1C8.7,5.1 8.3,5.5 8.3,5.9L7.9,8.7C7.3,8.9 6.7,9.2 6.2,9.6L3.6,8.5C3.2,8.4 2.7,8.5 2.5,8.9L0.5,12.3C0.2,12.7 0.3,13.2 0.7,13.5L3,15C3,15.3 3,15.7 3,16C3,16.3 3,16.7 3,17L0.7,18.5C0.3,18.8 0.2,19.3 0.5,19.7L2.5,23.1C2.7,23.5 3.2,23.6 3.6,23.5L6.2,22.4C6.7,22.8 7.3,23.1 7.9,23.3L8.3,26.1C8.3,26.6 8.7,26.9 9.2,26.9L13.2,26.9C13.7,26.9 14.1,26.5 14.1,26.1L14.5,23.3C15.1,23.1 15.7,22.8 16.2,22.4L18.8,23.5C19.2,23.6 19.7,23.5 19.9,23.1L21.9,19.7C22.2,19.3 22.1,18.8 21.7,18.5L19.4,17C19.4,16.7 19.4,16.3 19.4,16C19.4,15.7 19.4,15.3 19.4,15Z" Fill="#A0FFFFFF" Stretch="Uniform" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="退出" Click="TrayExit_Click">
                <MenuItem.Icon>
                    <Path Data="M19,3H5C3.89,3 3,3.89 3,5V9H5V5H19V19H5V15H3V19C3,20.11 3.89,21 5,21H19C20.11,21 21,20.11 21,19V5C21,3.89 20.11,3 19,3M10.08,15.58L11.5,17L16.5,12L11.5,7L10.08,8.41L12.67,11H3V13H12.67L10.08,15.58Z" Fill="#EF4444" Stretch="Uniform" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Application.Resources>
</Application>
