<Window x:Class="OpenQuickHost.MobileMessageToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手机消息"
        Width="390"
        Height="450"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        WindowStartupLocation="Manual">
    <Border Background="{DynamicResource BrushDialogBG}"
            BorderBrush="{DynamicResource BrushBorder}"
            BorderThickness="1"
            CornerRadius="18"
            Padding="16">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="28" ShadowDepth="8" Opacity="0.45" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0"
                        LastChildFill="False">
                <TextBlock x:Name="TitleText"
                           Text="手机"
                           Foreground="{DynamicResource BrushTextMain}"
                           FontSize="17"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           DockPanel.Dock="Left" />
                
                <Button DockPanel.Dock="Right"
                        Width="30"
                        Height="30"
                        Margin="8,0,0,0"
                        Content="×"
                        Style="{StaticResource SecondaryBtn}"
                        Padding="0"
                        FontSize="16"
                        Click="CloseButton_Click" />
                
                <Button DockPanel.Dock="Right"
                        Width="46"
                        Height="30"
                        Content="清理"
                        Style="{StaticResource SecondaryBtn}"
                        Padding="0"
                        FontSize="12"
                        Click="ClearHistoryButton_Click" />
            </DockPanel>

            <ScrollViewer x:Name="MessageScrollViewer"
                          Grid.Row="1"
                          Margin="0,14,0,0"
                          VerticalScrollBarVisibility="Auto"
                          Background="{DynamicResource BrushInputBG}">
                <StackPanel x:Name="MessageStack"
                            Margin="8" />
            </ScrollViewer>

            <DockPanel Grid.Row="2" Margin="0,10,0,0">
                <Button x:Name="AttachButton" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,8,0" Content="＋" Click="AttachButton_Click" Style="{StaticResource SecondaryBtn}" FontSize="16" Padding="0" />
                <Button x:Name="VoiceButton" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,8,0" Content="🎙" Click="VoiceButton_Click" Style="{StaticResource SecondaryBtn}" FontSize="14" Padding="0" />
                <Button x:Name="SendButton" DockPanel.Dock="Right" Width="60" Height="30" Margin="8,0,0,0" Content="发送" Click="SendButton_Click" Style="{StaticResource PrimaryBtn}" />
                <TextBox x:Name="InputTextBox" Height="30" Background="{DynamicResource BrushInputBG}" Foreground="{DynamicResource BrushTextMain}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="6,2,6,2" KeyDown="InputTextBox_KeyDown" VerticalContentAlignment="Center"/>
            </DockPanel>

            <TextBlock x:Name="UrlHintText"
                       Grid.Row="3"
                       Margin="2,8,0,0"
                       Foreground="{DynamicResource BrushAccent}"
                       FontSize="{DynamicResource FontSizeSmallPlus}"
                       TextTrimming="CharacterEllipsis"
                       Visibility="Collapsed" />

            <DockPanel Grid.Row="4"
                        Margin="0,10,0,0"
                        LastChildFill="False">
                <Button x:Name="OpenLinkButton"
                        Width="86"
                        Height="34"
                        DockPanel.Dock="Right"
                        Content="打开链接"
                        Style="{StaticResource PrimaryBtn}"
                        Click="OpenLinkButton_Click"
                        Visibility="Collapsed" />
            </DockPanel>
        </Grid>
    </Border>
</Window>
