<Window x:Class="OpenQuickHost.WindowPickerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WindowPickerOverlay" 
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        Cursor="Cross">
    <Canvas x:Name="OverlayCanvas">
        <!-- 蓝色遮罩：透明度0.3的蓝色填充，带更深颜色的蓝色边框 -->
        <Border x:Name="HighlightBorder" 
                BorderBrush="#FF007ACC" 
                BorderThickness="2" 
                Background="#22007ACC" 
                Visibility="Collapsed" />
        
        <!-- 左上角标题显示框：高档深色圆角卡片 -->
        <Border x:Name="InfoCard" 
                Background="#E61E1E1E" 
                CornerRadius="4" 
                Padding="10,6" 
                Margin="16" 
                BorderBrush="#44FFFFFF" 
                BorderThickness="1"
                Canvas.Left="0" 
                Canvas.Top="0">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🎯 正在选择目标窗口 (按 ESC 取消) | " Foreground="#FFA0A0A0" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="WindowTitleText" Text="无" Foreground="White" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock Text=" (" Foreground="#FFA0A0A0" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="ProcessNameText" Text="无" Foreground="#FF00BCF2" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock Text=")" Foreground="#FFA0A0A0" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Canvas>
</Window>
