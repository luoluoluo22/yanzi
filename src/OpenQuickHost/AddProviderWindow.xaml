<Window x:Class="OpenQuickHost.AddProviderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加模型服务提供商"
        Width="430"
        Height="310"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource DialogWindow}">
    <Border Margin="1"
            CornerRadius="0"
            Background="#F2161616"
            BorderBrush="#22FFFFFF"
            BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="添加提供商" Style="{StaticResource TitleText}" />
            
            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                <TextBlock Text="提供商名称" Foreground="#FFB8B8B8" FontSize="{DynamicResource FontSizeSmallPlus}" />
                <TextBox x:Name="ProviderNameBox"
                         Height="38"
                         Margin="0,6,0,0"
                         Padding="12,8"
                         VerticalAlignment="Center"
                         PreviewKeyDown="ProviderNameBox_PreviewKeyDown" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,12,0,0">
                <TextBlock Text="提供商类型" Foreground="#FFB8B8B8" FontSize="{DynamicResource FontSizeSmallPlus}" />
                <ComboBox x:Name="ProviderTypeBox"
                          Height="38"
                          Margin="0,6,0,0"
                          Style="{StaticResource DarkComboBoxStyle}"
                          VerticalContentAlignment="Center" />
            </StackPanel>

            <TextBlock x:Name="ErrorText"
                       Grid.Row="3"
                       Margin="0,10,0,0"
                       Foreground="#FFEF4444"
                       FontSize="{DynamicResource FontSizeSmallPlus}"
                       TextWrapping="Wrap"
                       Visibility="Collapsed" />

            <Grid Grid.Row="4" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1"
                        Content="取消"
                        Style="{StaticResource SecondaryBtn}"
                        Width="90"
                        Height="32"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click" />
                <Button Grid.Column="2"
                        Content="确定"
                        Style="{StaticResource PrimaryBtn}"
                        Width="90"
                        Height="32"
                        Click="OkButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
