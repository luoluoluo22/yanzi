<Window x:Class="OpenQuickHost.SkillExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导出 Skill"
        Width="460"
        Height="420"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindow}">
    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF2A2A2A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4"
                                Margin="4,2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#FF3B82F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="15"
            CornerRadius="14"
            Background="#FF161616"
            BorderBrush="#FF2A2A2A"
            BorderThickness="1"
            MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Direction="270" ShadowDepth="4" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="Auto" /> <!-- Content Separator -->
                <RowDefinition Height="*" />    <!-- Main Content -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- 1. Header Section -->
            <StackPanel>
                <TextBlock Text="导出 Skill" Style="{StaticResource TitleText}" />
                <TextBlock Margin="0,8,0,0"
                           Text="将软件内置的技能组同步到开发工具的 Skills 目录中。"
                           Style="{StaticResource DescriptionText}" />
            </StackPanel>

            <!-- 2. Main Content Section -->
            <StackPanel Grid.Row="2" Margin="0,25,0,0">
                <TextBlock Text="目标 Agent"
                           Foreground="#FFB8B8B8"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="2,0,0,8" />
                <ComboBox x:Name="TargetComboBox"
                          Style="{StaticResource DarkComboBox}"
                          SelectedIndex="0">
                    <ComboBoxItem Tag="Codex">Codex</ComboBoxItem>
                    <ComboBoxItem Tag="Antigravity">Antigravity</ComboBoxItem>
                    <ComboBoxItem Tag="Trae">Trae</ComboBoxItem>
                </ComboBox>

                <TextBlock Text="导出作用域"
                           Foreground="#FFB8B8B8"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="2,18,0,8" />
                <ComboBox x:Name="ScopeComboBox"
                          Style="{StaticResource DarkComboBox}"
                          SelectedIndex="0">
                    <ComboBoxItem Tag="Project">项目级</ComboBoxItem>
                    <ComboBoxItem Tag="Global">全局级</ComboBoxItem>
                </ComboBox>
                
                <TextBlock Margin="2,15,0,0"
                           Text="* 项目级导出将会在下一步要求选择目标项目根目录。"
                           Foreground="#4DFFFFFF"
                           FontSize="11"
                           TextWrapping="Wrap" />
            </StackPanel>

            <!-- 3. Footer Actions Section -->
            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="取消"
                        Style="{StaticResource SecondaryBtn}"
                        Margin="0,0,6,0"
                        Click="CancelButton_Click" />
                <Button Grid.Column="1"
                        Content="开始导出"
                        Style="{StaticResource PrimaryBtn}"
                        Margin="6,0,0,0"
                        Click="ConfirmButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
