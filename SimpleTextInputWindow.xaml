<Window x:Class="OpenQuickHost.SimpleTextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Width="420"
        Height="240"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindow}">
    <Border Margin="15"
            CornerRadius="12"
            Background="#FF161616"
            BorderBrush="#FF2A2A2A"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Text="输入" Style="{StaticResource TitleText}" />
            
            <TextBlock x:Name="DescriptionText"
                       Grid.Row="1"
                       Margin="0,8,0,0"
                       Style="{StaticResource DescriptionText}" />

            <TextBox x:Name="ValueBox"
                      Grid.Row="2"
                      Height="40"
                      Margin="0,20,0,0"
                      Padding="12,10"
                      VerticalAlignment="Top" />

            <Grid Grid.Row="3" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="ErrorText"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="#FFEF4444"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Visibility="Collapsed" />
                <Button Grid.Column="1"
                        Content="取消"
                        Style="{StaticResource SecondaryBtn}"
                        Width="90"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click" />
                <Button Grid.Column="2"
                        Content="确认"
                        Style="{StaticResource PrimaryBtn}"
                        Width="90"
                        Click="OkButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
