# 扩展管理界面重新设计说明

## 设计概述

基于参考界面 `插件管理参考界面.html` 的设计风格，将扩展管理界面从传统的表格列表布局重新设计为现代化的卡片式布局，并引入多标签筛选和右侧详情面板。

## 主要改进

### 1. 卡片式布局
- **从表格到卡片**：将原有的表格行布局改为卡片网格布局
- **视觉层次**：每个扩展以独立卡片形式展示，包含图标、标题、ID、徽章、版本等信息
- **响应式网格**：使用 `WrapPanel` 实现自适应的卡片网格，自动换行
- **悬停效果**：卡片支持悬停高亮，提升交互体验

### 2. 多标签筛选
- **筛选标签**：
  - 全部
  - 已发布
  - 已禁用
  - 快捷键
  - 回收站
- **视觉反馈**：激活的标签有明显的背景色区分
- **快速切换**：点击标签即可快速筛选不同类型的扩展

### 3. 右侧详情面板
- **三段式布局**：
  - **头部**：扩展图标、名称、版本、关闭按钮
  - **主体**：基本信息、运行统计、最近日志
  - **底部**：设置和立即运行按钮
- **详细信息展示**：
  - 基本信息：扩展 ID、分类、快捷键
  - 运行统计：总触发次数、成功率（带颜色区分）
  - 最近日志：时间戳和日志内容
- **可折叠**：支持显示/隐藏，不占用主内容区域

### 4. 设计细节

#### 颜色系统
```
背景色：
- 主背景：#FF161616
- 卡片背景：#FF16181C
- 次级背景：#FF1E1E1E
- 悬停背景：#FF1A1C20

边框色：
- 主边框：#FF2A2A2A
- 次级边框：#FF3A3A3A

文本色：
- 主文本：White
- 次级文本：#FF8E8E8E
- 暗文本：#FF6B7280

强调色：
- 蓝色（主要）：#FF3B82F6
- 绿色（成功）：#FF16A34A
- 黄色（警告）：#FFFBBF24
- 红色（错误）：#FFF87171
```

#### 卡片结构
```
┌─────────────────────────────┐
│ 📦  扩展名称          [开关] │
│     extension.id            │
│                             │
│ [已发布] [快捷键] [分类]    │
│                             │
│ ─────────────────────────── │
│ v1.0.0            [✏️] [📁] │
└─────────────────────────────┘
```

#### 详情面板结构
```
┌─────────────────────────────┐
│ 📦  扩展名称  v1.0.0    [✕] │
├─────────────────────────────┤
│ 基本信息                     │
│ ┌─────────────────────────┐ │
│ │ 扩展 ID    extension.id │ │
│ │ 分类       工具         │ │
│ │ 快捷键     Ctrl+Alt+E   │ │
│ └─────────────────────────┘ │
│                             │
│ 运行统计                     │
│ ┌──────────┐ ┌──────────┐  │
│ │  1284    │ │  99.2%   │  │
│ │ 总触发次数│ │  成功率  │  │
│ └──────────┘ └──────────┘  │
│                             │
│ 最近日志                     │
│ ┌─────────────────────────┐ │
│ │ 14:32  触发：Ctrl+Alt+E │ │
│ │ 14:28  触发：Ctrl+Alt+E │ │
│ │ 14:15  清理旧记录(42条) │ │
│ └─────────────────────────┘ │
├─────────────────────────────┤
│ [设置]          [立即运行]   │
└─────────────────────────────┘
```

## 实现文件

### 新增文件
- `src/OpenQuickHost/ExtensionCardView.xaml` - 完整的卡片式布局设计示例

### 修改文件
- `src/OpenQuickHost/SettingsWindow.xaml` - 扩展管理视图部分已更新为卡片式布局

## 需要实现的功能

### 前端交互
1. **卡片点击事件**：点击卡片展开右侧详情面板
2. **标签筛选**：点击标签切换筛选条件
3. **详情面板切换**：点击关闭按钮隐藏详情面板
4. **开关切换**：卡片上的开关控制扩展启用/禁用
5. **快捷操作**：编辑、打开目录等按钮功能

### 后端逻辑（需要在 SettingsWindow.xaml.cs 中实现）
```csharp
// 卡片点击事件
private void ExtensionCard_Click(object sender, MouseButtonEventArgs e)
{
    // 获取点击的扩展项
    // 显示右侧详情面板
    // 加载扩展详细信息
}

// 详情面板关闭
private void CloseDetailPanel_Click(object sender, RoutedEventArgs e)
{
    // 隐藏详情面板
    DetailPanelColumn.Width = new GridLength(0);
}

// 标签筛选
private void FilterTab_Click(object sender, MouseButtonEventArgs e)
{
    // 获取标签类型
    // 更新筛选条件
    // 刷新扩展列表
}
```

### 数据绑定属性（需要在 ViewModel 中添加）
```csharp
// 选中的扩展项（用于详情面板）
public SettingsExtensionItem? SelectedExtension { get; set; }

// 详情面板可见性
public bool IsDetailPanelVisible { get; set; }

// 扩展统计信息
public string ExtensionRunCount { get; set; }
public string ExtensionSuccessRate { get; set; }
public ObservableCollection<ExtensionLogItem> ExtensionLogs { get; set; }
```

## 设计优势

1. **视觉吸引力**：卡片式布局更现代、更美观
2. **信息密度**：在有限空间内展示更多扩展
3. **快速浏览**：徽章和图标帮助快速识别扩展类型
4. **详细查看**：右侧面板提供深入的扩展信息
5. **操作便捷**：常用操作直接在卡片上，详细操作在详情面板
6. **响应式**：自适应不同窗口大小

## 下一步工作

1. 完善 SettingsWindow.xaml 中的卡片布局实现
2. 实现右侧详情面板的显示/隐藏逻辑
3. 添加卡片点击事件处理
4. 实现标签筛选功能
5. 绑定扩展统计数据和日志数据
6. 添加动画效果（卡片悬停、详情面板滑入滑出）
7. 测试不同分辨率下的显示效果

## 参考资料

- 参考界面：`插件管理参考界面.html`
- 原始实现：`src/OpenQuickHost/SettingsWindow.xaml` (扩展管理部分)
- 设计示例：`src/OpenQuickHost/ExtensionCardView.xaml`
